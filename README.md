# Attention-Mechanism
This repository implements the Scaled Dot-Product Attention mechanism from scratch using Python and NumPy. It demonstrates how Query (Q), Key (K), and Value (V) matrices are generated, attention scores are computed, scaled, masked (optional), normalized using Softmax, and used to produce context vectors without relying on deep learning frameworks.
